// establish classes to pass fetched data to
// these are then used to show  / handle data on front-end

export class Posts {
    public key:string;
    public url:string;
    public name:string;
    public imageUrl:string;
}